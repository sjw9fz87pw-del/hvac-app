DATABASE_URL="REPLACE_ME"
NFC_TAG_SECRET="REPLACE_ME"
APP_BASE_URL="REPLACE_ME"
BLOB_DIR="REPLACE_ME"

# iPhone app (optional). Lets tapping a tag open the app instead of Safari;
# see docs/IOS-APP.md. IOS_BUNDLE_ID must equal the app's bundle id (CAP_APP_ID).
APPLE_TEAM_ID=""
IOS_BUNDLE_ID=""
